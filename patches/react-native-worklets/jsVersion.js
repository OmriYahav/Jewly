// Workaround for Expo Go including Worklets native module built with 0.6.0.
// Align the JavaScript bundle version with the native version to prevent
// mismatch errors during development.
export const jsVersion = '0.6.0';
